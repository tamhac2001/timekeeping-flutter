import 'package:flutter/material.dart';

const onTimeIcon = Icon(Icons.done, color: onTimeColor);
final earlyIcon = Icon(Icons.report_problem, color: earlyColor);
final lateIcon = Icon(Icons.report_problem, color: lateColor);
const forgotIcon = Icon(Icons.close, color: forgotColor);

final Color unknownColor = Colors.grey.shade300;
const MaterialColor onTimeColor = Colors.green;
final Color earlyColor = Colors.yellow.shade700;
final Color lateColor = Colors.yellow.shade700;
const MaterialColor forgotColor = Colors.red;

const approvedIcon = Icon(Icons.done, color: approvedColor);
const deniedIcon = Icon(Icons.close, color: deniedColor);

const MaterialColor approvedColor = Colors.green;
const MaterialColor deniedColor = Colors.red;
